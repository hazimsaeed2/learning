# EFS-02 Validation Addendum

Lecture: Week 17-1 EFS-02 Statistical Arbitrage and Pair trading Equity, FX & Futures Strategies

This file is additive. It preserves the original notebook, data files, and `chart_1_pairs.png`.

Local note: `statsmodels` is not installed in this environment, so the original notebook fails locally. The validated notebook adds small local `OLS` and `adfuller` fallbacks for execution validation.
